# ==============================================================
# = Authentication Server JWT
# = Profilo GIT
# ==============================================================
spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: file:///home/cristian/git_repo/udemy/git-config-repository
  config:
    activate:
      on-profile: git